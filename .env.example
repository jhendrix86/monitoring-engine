# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/monitoring

# Redis
REDIS_URL=redis://localhost:6379

# Prometheus
PROMETHEUS_URL=

# Monitoring intervals (seconds)
HEALTH_CHECK_INTERVAL=60
METRICS_COLLECTION_INTERVAL=30

# Drift monitoring: DriftMonitor background loop cadence (seconds) and the
# two comparison windows (hours). Current = trailing DRIFT_CURRENT_WINDOW_HOURS;
# baseline = DRIFT_BASELINE_WINDOW_HOURS ago up to the start of the current
# window. A metric that moves >=20% between the two opens a performance_drift alert.
DRIFT_CHECK_INTERVAL=300
DRIFT_CURRENT_WINDOW_HOURS=1
DRIFT_BASELINE_WINDOW_HOURS=24

# Alert thresholds (percent)
CPU_WARNING_THRESHOLD=80.0
CPU_CRITICAL_THRESHOLD=90.0
MEMORY_WARNING_THRESHOLD=80.0
MEMORY_CRITICAL_THRESHOLD=90.0

# Application
DEBUG=false

# Integration
NOTIFICATION_ENGINE_URL=http://localhost:8037

# Recipient for real alert notifications sent to notification-engine. Blank
# by default - notify_alert()/send_alert_notification() honestly skip
# sending until this is set, rather than notifying a fabricated address.
ALERT_NOTIFICATION_RECIPIENT=
